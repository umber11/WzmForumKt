package com.ls.user.config

object UserConfig {
    object AgreementType {
        const val KEY_AGREEMENT_TYPE = "KEY_AGREEMENT_TYPE"
        const val VALUE_AGREEMENT = 0
        const val VALUE_PRIVATE = 1
        const val VALUE_SIMPLE_PRIVATE = 2
    }

    object Camera_CAPTURE {
        const val CAPTURE_REQUEST = 1001
        const val KEY_CAPTURE_REQUEST = "KEY_CAPTURE_REQUEST"
    }
}
